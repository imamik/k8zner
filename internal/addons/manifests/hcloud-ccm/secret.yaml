# Hetzner Cloud API secret for Cloud Controller Manager
# This secret provides the CCM with credentials to interact with Hetzner Cloud API
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: hcloud
  namespace: kube-system
stringData:
  token: "{{.Token}}"
  network: "{{.NetworkID}}"

apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8zner-operator.fullname" . }}-metrics
  labels:
    {{- include "k8zner-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "k8zner-operator.selectorLabels" . | nindent 4 }}
